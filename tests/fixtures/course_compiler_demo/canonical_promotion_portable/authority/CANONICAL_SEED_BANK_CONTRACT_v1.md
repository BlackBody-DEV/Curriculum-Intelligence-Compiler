# TEST_FIXTURE canonical seed-bank contract

This newly authored, noncanonical fixture defines the public preparation-only
boundary exercised by compiler tests. A candidate retains its source identity,
content digest, procedure linkage, review evidence, and canonical IDs remain
unset. Database writes and student visibility are forbidden.
