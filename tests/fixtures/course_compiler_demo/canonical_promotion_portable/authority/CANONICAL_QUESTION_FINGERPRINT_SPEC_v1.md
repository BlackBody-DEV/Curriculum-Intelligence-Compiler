# TEST_FIXTURE fingerprint specification

The fixture fingerprint is a deterministic SHA-256 digest of normalized public
contract fields. Identity, structural, and content comparisons remain distinct;
no ordinal, host path, canonical ID, or student data contributes to the digest.
